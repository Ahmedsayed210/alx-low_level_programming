#include "m.h"
/**
 * main - Entry point of the program, invokes the print_school function
 *
 * Description:
 * This function serves as the starting point for the program. It calls the
 * print_school function, which is expected to handle the main logic of the
 * program. The purpose of the program is determined by the behavior
 * implemented within the print_school function.
 *
 * Return:
 * The function always returns EXIT_SUCCESS, indicating successful execution
 * of the program.
 */
int main(void)
{
	print_school();
	return (EXIT_SUCCESS);
}
